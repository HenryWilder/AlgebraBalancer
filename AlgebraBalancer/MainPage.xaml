<Page
    x:Class="AlgebraBalancer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlgebraBalancer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center" Grid.Column="0" Margin="18,0,18,0" Spacing="18" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBox x:Name="InputA" InputScope="Number" TextChanged="Update" TextAlignment="Right"/>
            <TextBox x:Name="InputB" InputScope="Number" TextChanged="Update" TextAlignment="Right"/>
        </StackPanel>
        <TextBlock FontSize="20" VerticalAlignment="Center" Grid.Column="1">=</TextBlock>
        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="18,0,18,0" Spacing="18">
            <TextBlock x:Name="Output" FontFamily="Consolas" FontSize="18"/>
        </StackPanel>
    </Grid>
</Page>
