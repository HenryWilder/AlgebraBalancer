<Page
    x:Class="AlgebraBalancer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlgebraBalancer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="11" Padding="8,0,0,0">© 2023 Henry Wilder</TextBlock>
            <VariableSizedWrapGrid Grid.Row="1" Orientation="Horizontal">
                <Button Background="{ThemeResource SystemControlAcrylicElementBrush}" Click="Button_Click" Content="𝑥"/>
                <Button Background="{ThemeResource SystemControlAcrylicElementBrush}" Click="Button_Click" Content="𝑦"/>
                <Button Background="{ThemeResource SystemControlAcrylicElementBrush}" Click="Button_Click" Content="𝑧"/>
                <Button Background="{ThemeResource SystemControlAcrylicElementBrush}" Click="Button_Click" Content="𝜋"/>
                <Button Background="{ThemeResource SystemControlAcrylicElementBrush}" Click="Button_Click" Content="𝜏"/>
            </VariableSizedWrapGrid>
            <TextBox x:Name="Notes" Grid.Row="2" AcceptsReturn="True" Padding="10,10,10,10" Margin="18,18,18,0"></TextBox>
            <StackPanel Grid.Row="3" Margin="18,18,18,18" Spacing="18" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBox Background="{ThemeResource SystemControlAcrylicElementBrush}" x:Name="InputA" TextChanged="Update" TextAlignment="Right"/>
                <TextBox Background="{ThemeResource SystemControlAcrylicElementBrush}" x:Name="InputB" TextChanged="Update" TextAlignment="Right"/>
                <TextBlock FontSize="20" Text="=" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="18,0,18,0" Spacing="18" MinWidth="100">
            <TextBlock x:Name="Output" FontFamily="Consolas" FontSize="18" Text="..."/>
        </StackPanel>
    </Grid>
</Page>
