<Page
    x:Class="AlgebraBalancer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlgebraBalancer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid VerticalAlignment="Center" Grid.Column="0" RowSpacing="18" Margin="18,0,18,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="InputA" InputScope="Number" TextChanged="Update" Grid.Row="0" Header="A" HorizontalAlignment="Right"/>
            <TextBox x:Name="InputB" InputScope="Number" TextChanged="Update" Grid.Row="1" Header="B" HorizontalAlignment="Right"/>
        </Grid>
        <TextBlock FontSize="20" VerticalAlignment="Center" Grid.Column="1">=</TextBlock>
        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="18,0,18,0">
            <TextBlock>Factors</TextBlock>
            <TextBlock x:Name="OutputFactors" Text="..." HorizontalAlignment="Left" FontFamily="Consolas"/>
        </StackPanel>
    </Grid>
</Page>
