<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlgebraBalancer.AlgebraOutput"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:System.Numerics"
    x:Class="AlgebraBalancer.AlgebraOutput.Exponent"
    mc:Ignorable="d"
    d:DesignHeight="64"
    d:DesignWidth="64">

    <Grid ColumnSpacing="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="Base" Grid.Row="1" Orientation="Horizontal" MinWidth="16" MinHeight="16"/>
        <StackPanel x:Name="Exponent" Grid.Column="1" Orientation="Horizontal" MinWidth="16" MinHeight="16" />
    </Grid>
</UserControl>
