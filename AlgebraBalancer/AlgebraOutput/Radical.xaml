<UserControl
    x:Class="AlgebraBalancer.AlgebraOutput.Radical"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlgebraBalancer.AlgebraOutput"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="64"
    d:DesignWidth="64">

    <StackPanel Orientation="Horizontal">
        <StackPanel x:Name="Coefficient" Orientation="Horizontal" MinWidth="16" MinHeight="16" VerticalAlignment="Center"/>
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="Index" Orientation="Horizontal" MinWidth="16" MinHeight="16"/>
            <TextBlock Grid.RowSpan="2" Grid.ColumnSpan="2" Text="⎷" HorizontalAlignment="Right"/>
        </Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <Line Stroke="White" X2="1" Stretch="Fill"/>
            <StackPanel x:Name="Radicant" Orientation="Horizontal" MinWidth="16" MinHeight="16"/>
        </StackPanel>
    </StackPanel>
</UserControl>
